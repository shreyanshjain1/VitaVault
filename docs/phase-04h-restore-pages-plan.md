# Phase 04H page restore hotfix

This hotfix restores the full Symptoms, Vitals, and Vaccinations page modules after an earlier import-only patch corrupted the files.

It also adds a tiny record focus helper so the pages can highlight a targeted card when opened with `?focus=<id>`.
